language: python
virtualenv:
  system_site_packages: true
env:
  matrix:
    - PYTHON_VERSION="3.8" FROM="setup.py"
    - PYTHON_VERSION="3.8" FROM="github"
    - PYTHON_VERSION="3.8" FROM="pypi"
install: source ./ci/.travis_install.sh
script: bash ./ci/.travis_test.sh
cache: apt
sudo: false
notifications:
  email:
    recipients:
      - mail@sebastianraschka.com
    on_success: always
    on_failure: always
